import 'package:equatable/equatable.dart';

abstract class ${NAME}Event extends Equatable {}

class ${NAME}State {

${NAME}State();

factory ${NAME}State.fromPrev({@required ${NAME}State prevState}) {
if (prevState == null) {
return ${NAME}State();
} else {
return ${NAME}State();
}
}
}